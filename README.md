# iOS ProgressView Update Value for UI 

1. mainThread -> working but update UI at last

2. performSelectorInBackground:(SEL)aSelector withObject:(nullable id)arg -> working

3. GCD dispatch_async -> not working
